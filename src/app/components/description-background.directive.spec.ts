import { DescriptionBackgroundDirective } from './description-background.directive';

describe('DescriptionBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new DescriptionBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
